---
description: 
globs: 
alwaysApply: false
---
when writing a .sh script, add this to the beginning of the file:

# Get the directory where the script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
